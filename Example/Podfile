use_frameworks!

target 'SwiftyForms_Example' do
	pod 'SwiftyForms', :path => '../'
	
	target 'SwiftyForms_Tests' do
		inherit! :search_paths
		pod 'Quick', '~> 0.10.0'
		pod 'Nimble', '~> 5.0.0'
	end
end
